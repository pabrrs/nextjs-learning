import { Noto_Sans, Lusitana } from 'next/font/google';
 
export const notoSans = Noto_Sans({ subsets: ['latin'], weight: '600' });
export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' });